pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "JavaDowngrader"

include(":standalone")
include(":runtime-dep")
include(":impl-classtransform")
include(":gradle-plugin")
